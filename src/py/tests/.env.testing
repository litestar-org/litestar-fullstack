# App
SECRET_KEY='secret-key'

# Database - use sqlite for testing to avoid PostgreSQL dependency during import
DATABASE_URL='sqlite:///:memory:'
DATABASE_ECHO=false
DATABASE_ECHO_POOL=false

SAQ_USE_SERVER_LIFESPAN=False # don't use with docker.
SAQ_WEB_ENABLED=True
SAQ_PROCESSES=1
SAQ_CONCURRENCY=1

VITE_PORT=3006
VITE_DEV_MODE=True

# Email - disable for testing
EMAIL_ENABLED=false
