{
  "$schema": "https://schema.up.railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "app database upgrade --no-prompt && app run --wc 2 --host 0.0.0.0 --port $PORT"
  }
}
