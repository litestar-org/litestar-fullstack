version: "3"

services:
  postgres:
    image: postgres
    ports:
      - "5423:5432"
    environment:
      POSTGRES_PASSWORD: super-secret

  redis:
    image: redis
    ports:
      - "6397:6379"
